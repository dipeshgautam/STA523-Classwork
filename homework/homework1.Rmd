---
title: "Homework1"
author: Andrew Wong, Heather Shapiro, Jessie Li, Dipesh Gautam
output: html_document
---

#Variables to Find
```{r}
id <-  # jessie
pig <-  #jessie 
class <- #jessie
pid <- #heather 
mid <- #heather
pname <- #heather
pteam <- #dipesh
ptime <- #dipesh
paddtime <- #dipesh
ptype <- #dipesh
pteamimg <- #andrew
pmatch <- #andrew
style <- #andrew 
```

```{r}


d=readLines("~cr173/Sta523/data/world_cup_goals_raw.txt")
library(stringr)

pid <- unlist(str_match_all(d," pid=\"([0-9]*)\""))[seq(2,194,2)]
mid <- unlist(str_match_all(d," mid=\"([0-9]*)\""))[seq(2,194,2)]
name <- unlist(str_match_all(d, ignore.case(" pname=\"([a-z &;']*)\"")))[seq(2,194,2)]

#pteam=\"Cameroon\"
team=unlist(str_match_all(d, ignore.case(" pteam=\"([a-z &;']*)\"")))[seq(2,194,2)]


#ptime=\"26\" 
time=unlist(str_match_all(d, " ptime=\"([0-9]*)\""))[seq(2,194,2)]


#paddtime=\"0\" 
addedTime=unlist(str_match_all(d, " paddtime=\"([0-9]*)\""))[seq(2,194,2)]

#ptype=\"goal\"
goalType=unlist(str_match_all(d, ignore.case(" ptype=\"([a-z]*[a-z -]*)\"")))[seq(2,194,2)]


```
